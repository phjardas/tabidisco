{
  "name": "tabidisco",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "yarn workspaces foreach run build",
    "start": "yarn workspaces foreach --parallel --interlaced run dev",
    "clean": "yarn workspaces foreach --parallel run clean"
  },
  "workspaces": [
    "gui",
    "server"
  ],
  "engines": {
    "node": "11"
  }
}
