{
  "name": "tabidisco",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run --parallel start",
    "clean": "lerna run --parallel clean"
  },
  "workspaces": [
    "gui",
    "server"
  ],
  "engines": {
    "node": "11"
  },
  "devDependencies": {
    "lerna": "^3.19.0"
  }
}
